sch_profiles = Profils d'horaires
menu.schedules_profiles = {:sch_profiles:}
sch_profiles.validate.wrong_expression = %s n'est pas une expression cron valide
sch_profiles.desc.title = Description de ce profil d'horaire.
sch_profiles.profile = Modèle
sch_profiles.profile.title = Assistant rapide de modèle
sch_profiles.schedule = Horaire
sch_profiles.minute = Minute
sch_profiles.hour = Heure
sch_profiles.day = Jour du Mois
sch_profiles.month = Mois
sch_profiles.weekday = Jour de la Semaine
sch_profiles.task.title = :type pour exécuter la tâche
sch_profiles.select_tpl = -- Sélectionner un modèle --
sch_profiles.tpl.once_per_minute = Une fois par minute (* * * * *)
sch_profiles.tpl.once_each_five = Une fois toutes les cinq minutes (*/5 * * * *)
sch_profiles.tpl.twice_per_hour = Deux fois par heure (0,30 * * * *)
sch_profiles.tpl.once_per_hour = Une fois par heure (0 * * * *)
sch_profiles.tpl.twice_per_day = Deux fois par jour (0 0,12 * * *)
sch_profiles.tpl.once_per_day = Une fois par jour (0 0 * * *)
sch_profiles.tpl.once_per_week = Une fois par semaine (0 0 * * 0)
sch_profiles.tpl.1st_15th = Le 1er et le 15 de chaque mois (0 0 1,15 * *)
sch_profiles.tpl.once_per_month = Une fois par mois (0 0 1 * *)
sch_profiles.tpl.once_per_year = Une fois par an (0 0 1 1 *)
sch_profiles.quick_templates = -- Modèles rapides --
sch_profiles.qtpl.each_minute = Chaque minute (*)
sch_profiles.qtpl.each_two_minutes = Toutes les deux minutes (*/2)
sch_profiles.qtpl.each_five_minutes = Toutes les cinq minutes (*/5)
sch_profiles.qtpl.each_ten_minutes = Toutes les dix minutes (*/10)
sch_profiles.qtpl.each_fifteen_minutes = Toutes les quinze minutes (*/15)
sch_profiles.qtpl.each_thirty_minutes = Toutes les trente minutes (0,30)
sch_profiles.qtpl.every_hour = Chaque heure (*)
sch_profiles.qtpl.each_two_hours = Toutes les deux heures (*/2)
sch_profiles.qtpl.each_three_hours = Toutes les trois heures (*/3)
sch_profiles.qtpl.each_four_hours = Toutes les quatre heures (*/4)
sch_profiles.qtpl.each_six_hours = Toutes les six heures (*/6)
sch_profiles.qtpl.each_twelve_hours = Toutes les douze heures (0,12)
sch_profiles.qtpl.every_day = Chaque jour (*)
sch_profiles.qtpl.every_two_days = Tous les deux jours (*/2)
sch_profiles.qtpl.1st_15th = Le 1er et le 15 du mois (1,15)
sch_profiles.qtpl.every_month = Chaque mois (*)
sch_profiles.qtpl.every_two_months = Tous les deux mois (*/2)
sch_profiles.qtpl.every_three_months = Tous les trois mois (*/3)
sch_profiles.qtpl.every_six_months = Tous les six mois (*/6)
sch_profiles.qtpl.every_weekday = Chaque jour de la semaine (1-5)
sch_profiles.qtpl.every_weekend = Chaque week-end (6,0)
sch_profiles.qtpl.specific_days = Chaque lundi, mercredi et vendredi (1,3,5)
sch_profiles.qtpl.specific_days_2 = Chaque mardi et jeudi (2,4)
sch_profiles.tpl.on_sunday = À 00:00 le dimanche (0 0 * * 0)
sch_profiles.tpl.on_saturday = À 00:00 le samedi (0 0 * * 6)
sch_profiles.select_sch = -- Sélectionner un horaire --